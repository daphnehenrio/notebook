// ? Import NPM
// | Material Components
import {
  ListItemIcon,
} from '@mui/material';
// | Material Styles
import { styled } from '@mui/material/styles';

// ? Export
export default styled(ListItemIcon)(() => ({
  minWidth: 'unset',
  marginRight: '5px',
}));
