// ? Import NPM
// | Material Components
import {
  Typography,
} from '@mui/material';

// | Material Styles
import { styled } from '@mui/material/styles';

// ? Export
export default styled(Typography)(() => ({
  display: 'flex',
  alignItems: 'center',
}));
